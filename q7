#include<bits/stdc++.h>
using namespace std;
int main()
{
    cout<<"reverse case even numbers from 24 to 4:"<<endl;
    int i=24;
    while(i>=4){
        if(i%2==0)
        {
    cout<<i<<endl; }
    i--;
    }
    return 0;
}
